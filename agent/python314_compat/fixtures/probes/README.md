# Probe Fixtures

Keep ordinary executable compatibility fixtures in `tests/fixtures`.

Use this folder only for agent-loop metadata about CPython standard-library
probe groups, not for normal test cases. This avoids splitting the real test
suite across multiple locations.
